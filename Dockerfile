# FROM nginx:alpine

# COPY dist/gateway-manager/browser /usr/share/nginx/html

# COPY nginx.conf /etc/nginx/conf.d/default.conf

# # Expose port 80
# EXPOSE 80

# # Start Nginx server
# CMD ["nginx", "-g", "daemon off;"]

